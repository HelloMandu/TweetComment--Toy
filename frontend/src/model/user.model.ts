export type UserModel = {
  name: string;
  username: string;
  url?: string;
};

export type UserInfo = {
  id: string;
  name: string;
  username: string;
  password: string;
  email: string;
  url?: string;
};
